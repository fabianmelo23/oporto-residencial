{
  "name": "oporto-residencial",
  "version": "1.0.0",
  "description": "Oporto Residencial — plataforma de gestión residencial",
  "main": "server.js",
  "scripts": {
    "setup": "node -e \"const fs=require('fs'); if(!fs.existsSync('.env')) fs.copyFileSync('.env.example','.env');\" && npm install",
    "start": "node server.js",
    "dev": "nodemon --ignore data/ server.js",
    "test": "node --test test/api.test.js"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "dotenv": "^16.4.5",
    "express": "^4.21.0"
  },
  "devDependencies": {
    "nodemon": "^3.1.0"
  }
}
